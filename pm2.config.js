const { Dashboard } = require('@mui/icons-material')

module.exports = {
  apps: [
    {
      name: 'ruuvi-dashboard',
      script: 'npm',
      args: 'start',
      watch: true, // (optional)
    },
  ],
}
