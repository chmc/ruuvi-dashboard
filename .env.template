#TEST=true
# Backend server port (used by Express)
BACKEND_PORT=3001

VITE_RUUVITAG_MACS=mac1,mac2,mac3,mac4
VITE_MAIN_INDOOR_RUUVITAG_MAC=mac1
VITE_MAIN_OUTDOOR_RUUVITAG_MAC=mac2
VITE_RUUVITAG_NAMES=Living room,Outdoor,Bedroom,Bathroom
VITE_OPENWEATHERMAP_APIKEY=your-key

# History Database
# RUUVI_HISTORY_DB_PATH=./server/ruuviHistory.db

# Buffer flush interval in seconds (default: 900 = 15 minutes)
# RUUVI_BUFFER_FLUSH_INTERVAL=900

# Seed history database with 90 days of fake data (development only)
# RUUVI_SEED_HISTORY=true

# SD Card Protection - Optional tmpfs buffer
# When enabled, buffer is persisted to RAM-based filesystem for service restart recovery
# RUUVI_USE_TMPFS_BUFFER=true
# RUUVI_TMPFS_PATH=/var/ruuvi-buffer
